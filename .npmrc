# The repo lives on a WSL 9P mount (/mnt/c). Symlinked node_modules are slow and
# unreliable there, so hoist and copy instead. See implement.md §1.
node-linker=hoisted
package-import-method=copy
prefer-frozen-lockfile=false
